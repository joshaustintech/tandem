# tandem plan

Goal: keep `tandem` as the smallest Elm + Tailwind + Vite starter, but use `rs-vite-plugin-elm` instead of `vite-plugin-elm`.

## Current slice

- Replace the plugin import and package dependency.
- Keep the starter lean enough to reuse as a base for `lambdadraw`.
