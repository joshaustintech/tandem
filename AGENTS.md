# Agent instructions

Current phase: implementation.

Read `PLAN.md`, `AGENT_HARNESS.md`, and this file before editing.

Rules:

- Use `rs-vite-plugin-elm` for Elm/Vite work here.
- Keep changes tiny and test-driven.
- Do not touch `excalidraw/` or `vite-plugin-elm/`.
- Prefer the smallest proof command that covers the edit.
- Run `npm test` or `npm run build` before claiming success.
